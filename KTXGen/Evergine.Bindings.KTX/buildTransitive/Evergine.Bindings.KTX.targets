<Project DefaultTargets="Build" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">

	<!-- Note: In theory you shouldn't need to explicitly do this NativeFileReference anymore.
	It should automatically pick up the native libraries following the folders naming conventions.
	This is only needed because the is an issue in dotnet -->
	<ItemGroup Condition="$(RuntimeIdentifier) == 'browser-wasm'">
	    <NativeFileReference Include="$(MSBuildThisFileDirectory)..\runtimes\browser-wasm\native\ktx.a" ScanForPInvokes="true" />
	</ItemGroup>

	<ItemGroup Condition="'$(TargetPlatformIdentifier)' == 'ios' And '$(RuntimeIdentifier)' != 'iossimulator-arm64'">
	    <NativeReference Include="$(MSBuildThisFileDirectory)native\ios-arm64\ktx.a">
		  <Kind>Static</Kind>
		  <IsCxx>True</IsCxx>
		  <ForceLoad>True</ForceLoad>
	    </NativeReference>
	</ItemGroup>

	<ItemGroup Condition="'$(TargetPlatformIdentifier)' == 'ios' And '$(RuntimeIdentifier)' == 'iossimulator-arm64'">
	    <NativeReference Include="$(MSBuildThisFileDirectory)native\iossimulator-arm64\ktx.a">
		  <Kind>Static</Kind>
		  <IsCxx>True</IsCxx>
		  <ForceLoad>True</ForceLoad>
	    </NativeReference>
	</ItemGroup>

</Project>
